If you want to run a pod, on every node. Then, we use Daemon set. They, make sure that an instance of pod
runs on every node.
- Monitoring Agent [node exporter]
- Log collector [fluentd]

kind: Daemonset