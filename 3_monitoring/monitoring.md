We will need either a proper monitoring solution 

- prometheus, grafana and node exporter

or to see things in realtime, we can use metric server to run commands like
kubectl top nodes or top pods

For logs, we have the following stack.

- EFK